name: Deploy to Vercel

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel deploy
        run: |
          curl -sS -o /tmp/resp.txt -w "%{http_code}" -X POST "${{ secrets.VERCEL_DEPLOY_HOOK_URL }}"
